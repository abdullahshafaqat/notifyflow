info:
  name: "send notification "
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/send
  body:
    type: json
    data: '{"to":"test@gmail.com","message":"hello"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
